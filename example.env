# Pi-hole DNS updater configuration (.env)

# Required: CSV list of Pi-hole hosts (IPs or hostnames)
# Example: 10.10.0.2,10.10.0.3
PIHOLES="10.10.0.2,10.10.0.3"

# Optional: Use HTTPS for the API (true/false). Default: false
USE_HTTPS=false

# Optional: TLS verification. "-k" to allow self-signed; empty string to enforce verification
INSECURE="-k"

# Password sources (choose one or mix):
# 1) Per-host env variables (recommended if not using Keychain)
#    Construct variable name as PIHOLE_PASSWORD_<HOST>, non-alnum -> _
#    Example for host 10.10.0.2:
# PIHOLE_PASSWORD_10_10_0_2=

# 2) CSV list aligned with PIHOLES (least secure)
# PASSWORDS=

# 3) macOS Keychain service name (recommended on macOS)
#    Create entries with:
#      security add-generic-password -s pihole-api -a 10.10.0.2 -w <password>
#    Then set KEYCHAIN_SERVICE below to pihole-api
# KEYCHAIN_SERVICE=
